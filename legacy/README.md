# Legacy Cluster Chat Archive

This directory is read-only rollback material for the former cluster-chat
application. It is intentionally excluded from the default CMake graph,
Gateway runtime, setup scripts, migrations, and deployment assets.

The active project identity is **AI Gateway**. Restore the Phase 6 Git baseline
to run the archived ChatServer/ChatClient targets; do not mix the legacy schema
or credentials with Gateway configuration.
